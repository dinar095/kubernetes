apiVersion : v1
kind : Pod
metadata:
  name: my-web
  lables:
    env : prod
    app : nginx
    tier : frontend
    owner : desausag
spec:
  containers:
    - name: my-server
      image: nginx
      imagePullPolicy: Never
      ports:
        - containerPort: 443